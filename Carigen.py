from abc import ABC
from car import Car
import array as arr
class Carigen(Car,ABC):
    def __init__(self, last_service_date):
        super().__init__(last_service_date)
       
        for x in range(i=0):
          arr[i]=int(input())
          ele = int(input())
          if arr[i]>0.9: 
            print("Needs To be Serviced")
